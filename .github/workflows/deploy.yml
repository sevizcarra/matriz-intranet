{"name":"Build & Deploy","on":{"push":{"branches":["main"],"paths":["matriz-intranet/**"]},"workflow_dispatch":{}},"permissions":{"contents":"write"},"jobs":{"deploy":{"runs-on":"ubuntu-latest","steps":[{"uses":"actions/checkout@v4"},{"uses":"actions/setup-node@v4","with":{"node-version":20}},{"name":"Instalar dependencias","run":"npm ci","working-directory":"matriz-intranet"},{"name":"Compilar","run":"npm run build","working-directory":"matriz-intranet"},{"name":"Publicar en docs","run":"rm -rf docs/assets && cp -r matriz-intranet/dist/assets docs/assets && cp matriz-intranet/dist/index.html docs/index.html && if [ -n \"$(git status --porcelain docs)\" ]; then git config user.name github-actions && git config user.email github-actions@users.noreply.github.com && git add docs && git commit -m 'Deploy automatico desde Actions' && git push; fi"}]}}}
